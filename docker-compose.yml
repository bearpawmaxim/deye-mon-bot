services:
  deye-mon-bot-rds:
    image: redis:7
    container_name: deye-mon-bot-rds
    ports:
      - "6379:6379"
    volumes:
      - rds_data:/data
    command: ["redis-server", "--appendonly", "yes"]
    networks:
      - web_network
    labels:
      io.hass.type: "addon"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  deye-mon-bot-mongo:
    image: mongo:7
    container_name: deye-mon-bot-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - web_network
    env_file: .env
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s

  deye-mon-bot-back-end:
    build:
      context: .
      dockerfile: back-end/Dockerfile
    image: deye-mon-bot-api
    restart: always
    env_file: .env
    environment:
      - DEBUG=False
    volumes:
      - ./back-end/db.sqlite3:/deye-mon-bot-api/db.sqlite3
    expose:
      - "5005"
    networks:
      - web_network
    labels:
      io.hass.type: "addon"
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; s=socket.socket(); s.settimeout(1); result=s.connect_ex(('localhost', 5005)); s.close(); exit(0 if result==0 else 1)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    depends_on:
      deye-mon-bot-mongo:
        condition: service_healthy
      deye-mon-bot-migrate:
        condition: service_completed_successfully
      deye-mon-bot-rds:
        condition: service_healthy

  deye-mon-bot-migrate:
    image: deye-mon-bot-api
    container_name: deye-mon-bot-migrate
    env_file: .env
    command:
      [
        "beanie",
        "migrate",
        "-uri", "${MONGO_URI}",
        "-db", "${MONGO_DB}",
        "-p", "migrations/",
        "--no-use-transaction"
      ]
    depends_on:
      deye-mon-bot-mongo:
        condition: service_healthy
    networks:
      - web_network
    restart: "no"

  deye-mon-bot-sse-back-end:
    build:
      context: .
      dockerfile: sse-back-end/Dockerfile
    image: deye-mon-bot-sse-api
    restart: always
    env_file: .env
    environment:
      - DEBUG=False
    expose:
      - "5005"
    networks:
      - web_network
    labels:
      io.hass.type: "addon"
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; s=socket.socket(); s.settimeout(1); result=s.connect_ex(('localhost', 5005)); s.close(); exit(0 if result==0 else 1)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    depends_on:
      deye-mon-bot-rds:
        condition: service_healthy

  deye-mon-bot-grid-reporter:
    build:
      context: grid-reporter
      dockerfile: Dockerfile
    image: deye-mon-bot-grid-reporter
    restart: always
    env_file: .env
    environment:
      CLIENTS_PATH: /app/clients.json
      STATE_PATH: /app/state.json
    volumes:
      - ./grid-reporter/clients.json:/app/clients.json
      - ./grid-reporter/state.json:/app/state.json
    networks:
      - web_network

  deye-mon-bot-front-end:
    build:
      context: front-end
      additional_contexts:
        shared: ./shared
    image: deye-mon-bot-ui
    restart: always
    ports:
      - "5005:80"
    networks:
      - web_network
    labels:
      io.hass.type: "addon"
    depends_on:
      deye-mon-bot-back-end:
        condition: service_healthy
      deye-mon-bot-sse-back-end:
        condition: service_healthy

volumes:
  rds_data:
  mongo_data:

networks:
  web_network:
    driver: bridge
